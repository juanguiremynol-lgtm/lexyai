/**
 * Platform PDF Settings Page
 * Super Admin only — configure Gotenberg endpoint for PDF generation
 */

import { PlatformPdfSettingsTab } from "@/components/platform/tabs/PlatformPdfSettingsTab";

export default function PlatformPdfSettingsPage() {
  return <PlatformPdfSettingsTab />;
}
